import { createApp } from "vue";
import "./style.css"; // Import your CSS file if needed
import App from "./App.vue";

// Create the Vue app instance and mount it to the DOM element with id 'app'
createApp(App).mount("#app");
